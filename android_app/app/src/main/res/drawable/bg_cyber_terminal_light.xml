<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Light gradient background -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="#F8FAFC"
                android:centerColor="#FFFFFF"
                android:endColor="#E2E8F0"
                android:type="linear" />
        </shape>
    </item>

    <!-- Subtle grid overlay (lighter) -->
    <item android:drawable="@drawable/grid_pattern_light" />

    <!-- Very subtle noise texture (lighter) -->
    <item android:drawable="@drawable/noise_texture_light" />
</layer-list>